# Vistynq — документация

**Vistynq** — веб-приложение для работы с заметками как с графом знаний: каждая заметка живёт на бесконечном канвасе в виде ноды, заметки связываются друг с другом прямыми и косвенными ссылками, навигация строится на тегах, важности и наглядных связях.

## Основные концепты

| Понятие | Описание |
|---|---|
| **Канвас** | Бесконечное графическое поле. У пользователя может быть неограниченно много канвасов. |
| **Заметка (нода)** | Единица контента. Всегда принадлежит ровно одному канвасу. На канвасе отображается прямоугольной нодой, на которой виден только заголовок. |
| **Манифест заметки** | Структурированные метаданные: заголовок, теги, атрибуты, прямые ссылки. Хранится в БД, редактируется через UI-форму (не как код). |
| **Тело заметки** | Markdown-текст с wiki-ссылками вида `[[...]]`. |
| **Прямая ссылка** | Связь, заданная вручную на канвасе. Сплошная линия со стрелкой. |
| **Косвенная ссылка** | Связь из `[[wikilink]]` внутри markdown-текста. Пунктирная линия со стрелкой. |
| **Атрибуты** | Важность 1–5 (по умолчанию 3), актуальность (актуальна / «неактуально» → в архив). |
| **Теги** | Локальны для канваса, имеют цвет, переименовываются и удаляются централизованно. |
| **Доступ** | Три уровня у канваса и заметки: `private` (только создатель), `specific` (конкретные пользователи по email-инвайту, роли viewer/editor), `public_link` (все, включая анонимов, только чтение). По умолчанию — **private**. |

## Стек

- **Backend:** ванильный PHP ≥ 8.2 (без фреймворка), PostgreSQL, REST JSON API, JWT (access в памяти клиента + refresh в httpOnly-cookie), S3-совместимое хранилище (MinIO), Composer-пакеты: `nikic/fast-route`, `firebase/php-jwt`, `aws/aws-sdk-php`, `phpmailer/phpmailer`.
- **Frontend:** Vue 3 + TypeScript + Vite, Pinia, Vue Router, vue-i18n (RU/EN), свой CSS без UI-библиотек, канвас — Vue Flow, редактор — Tiptap.
- **Инфраструктура:** docker compose (nginx + php-fpm, postgres, minio, mailpit).

## Разделы документации

1. [Функциональные требования](requirements.md) — роли, доступы, требования к продукту и NFR.
2. [Архитектура](architecture.md) — компоненты, потоки данных, структура кода.
3. [Модель данных](data-model.md) — таблицы, поля, связи, ER-диаграмма, жизненный цикл заметки.
4. [API Reference](api.md) · [OpenAPI-спецификация](openapi.yaml) — эндпоинты, матрица прав, ошибки.
5. [UI/UX-спецификация](ui-ux.md) — экраны, контекстные меню, поведение канваса.
6. [Гайд пользователя](user-guide.md) — сценарии от регистрации до шеринга.
7. [Roadmap](ROADMAP.md) — приоритизированный план реализации с детализацией этапов.

## Roadmap реализации

Актуальный план с приоритетами (P0–P3), зависимостями и критериями готовности — в [ROADMAP.md](ROADMAP.md).
Обзорная последовательность этапов по продукту:

| Этап | Содержание |
|---|---|
| 0 | docker compose, конфиги, миграционный раннер, скелет PHP-приложения (роутер, DI, middleware, ошибки), seed |
| 1 | Регистрация, email-верификация, login/refresh (httpOnly cookie), reset-пароля, профиль и аватар |
| 2 | CRUD канвасов и заметок, манифест, теги, архив, поиск по заголовкам/тегам |
| 3 | Канвас на Vue Flow: ноды, drag/resize/зум, контекстное меню, фильтры важности, подсветка тегов |
| 4 | Ссылки: прямые (в т.ч. «создать от этой заметки»), косвенные через `[[...]]`, битые ссылки |
| 5 | Загрузка файлов: картинки в текст, аудио с плеером, .txt/.md → заметка, drop на канвас, S3/MinIO |
| 6 | Доступы: инвайты по email, роли viewer/editor, публичные read-only ссылки, приватность графа |
| 7 | i18n, полировка состояний, тесты сервисов (LinkParser, AccessService, Jwt) |

## Вне скоупа v1 (бэклог v2)

Realtime-коллаборация, история версий заметок, полнотекстовый поиск по markdown-телу (Postgres FTS), мобильная версия.

## Быстрый старт (dev)

```bash
cp .env.example .env                       # сменить пароли и JWT_SECRET
docker compose up -d                       # postgres, minio (+init бакета), mailpit, nginx+php-fpm, frontend (vite)
curl http://localhost:8000/api/v1/healthz  # {"status":"ok"}
# фронт: http://localhost:5173 · API: http://localhost:8000/api/v1 · Mailpit: http://localhost:8025 · MinIO console: http://localhost:9001
# миграции и seed (появятся в R05): docker compose exec backend php bin/migrate · bin/seed
```
